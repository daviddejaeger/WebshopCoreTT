@using Microsoft.AspNetCore.Mvc.Localization
@model WebshopCoreTT.Models.ViewModels.RubberViewModels.IndexViewModel

@inject IViewLocalizer Localizer

<h2>@Localizer["Rubbers"]</h2>

<div class="row">
    <div class="col-sm-3">
        <div>
            <h4 class="font-weight-bold">@Localizer["Merk"]</h4>
            <form action="#">
                @foreach (var rubber in Model.Rubbers)
                {

                }
                @*<div *ngFor='let brand of brands' class="form-group">
                        <input id="calvin" name="clothes-brand" checked="" type="checkbox">
                        <label for="calvin">{{brand?.name}}<span class="badge">{{brand?.count}}</span></label>
                    </div>*@
            </form>
        </div>
    </div>
</div>

@if (Model.PromoProducts.Count() >= 1)
{
    <div class="promos">
        <h4>@Localizer["Promos"]</h4>
        <br />
        <div class="row">
            @foreach (var product in Model.PromoProducts)
            {
                <div class="col-sm-6 col-lg-3">
                    <div class="card w-100">
                        <a asp-controller="Home" asp-action="Index">
                            <img class="card-img-top" src=@product.ImagePath alt="product-img">
                        </a>
                        <div class="card-body">
                            <h5 class="card-title">@product.Name</h5>
                            @if (product.OnSale)
                            {
                                <p class="card-text linethrough d-inline-block">€@product.Price</p>
                                <p class="d-inline-block red"> €@product.SalesPrice</p>
                            }
                            else
                            {
                                <p class="card-text">€@product.Price</p>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
    <br>
}

@if (Model.NewProducts.Count() >= 1)
{
    <div class="new">
        <h4>@Localizer["Nieuw"]</h4>
        <div class="row">
            @foreach (var product in Model.NewProducts)
            {
                <div class="col-sm-6 col-lg-3">
                    <div class="card w-100">
                        <a asp-controller="Home" asp-action="Index">
                            <img class="card-img-top" src=@product.ImagePath alt="product-img">
                        </a>
                        <div class="card-body">
                            <h5 class="card-title">@product.Name</h5>
                            @if (product.OnSale)
                            {
                                <p class="card-text linethrough d-inline-block">€@product.Price</p>
                                <p class="d-inline-block red"> €@product.SalesPrice</p>
                            }
                            else
                            {
                                <p class="card-text">€@product.Price</p>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}
